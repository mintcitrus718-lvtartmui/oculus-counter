//笔记页！！运行不了！！ （吗？ 

#include <iostream>		//头文件 引入外部库 （输入输出流） 
#include <cmath> 		//引入数学库
#include <ctime> 		//时间库 

using namespace std; 	//把所有std全部总和（不用写了

	int a = 0; 				//创建整数变量 4字节 将a赋值为0 		*写在主函数外面的是全局变量 
	int a,b,c = 0;			//同时定义abc三个 
	double b; 				//双精度浮点数（小数）变量 
	float e;				//单精度浮点数变量 （小数后6位 
	string c;				//字符串变量 
	char d;					//字符变量（内容大小不超过一字节
	bool f;					//布尔变量（true/false） 
	long long				//长整型 8字节 
	int o[5];				//数组 一次性定义5个连续int变量 全局默认所有数据为0
	int a[100]={};			//手动全部赋值为0
	int b[2]={23,23};		//给a0,a1赋值

	
	int c[100];
	memset(c,0,sizeof(c));	//定义数组大小，一键赋值为0 
	
	const int maxn = 1001;	//定义常变量maxn，不可修改变量maxn的值 
	int r[maxn];

	#define N 1001 			//宏变量，凡N即用1001替代 
	
int main (){			//主函数 （只能有一个 
	
	cin >> b;			//输入赋值变量b
	
	cout << a << endl;	//输出变量a的值，endl为换行符
	cout << "AAAAA/n";	//输出"字符串"， /n为换行符 

	printf("%.3lf",b);  //输出b 保留三位小数 

	//如果新定义一个int 
	cout << n/100 << " " ;		//n的最高位是百位所以直接除，如果百位不是最高位则以此类推。（所以这个程序仅支持3位数） 
	cout << n/10%10 << " ";		//n缩小十倍（去除个位）再向10取余 *取十位 
	cout << n%10 << " ";		//n向10取余（提取n/10的余数） 	  *取个位  
	
	//cmath 
	cout << ceil(b);			//向上取整b
	cout << floor(b);			//向下取整b
	sprt(A);					//开平方 
	
	//ctime
	srand(time (0)); 			//初始化随机数生成器的表达式
	int i = rand() % 100 + 1;	//随机生成1~100的整数并赋值给i
	int j = rand() % 100:		//0~99
	
	//关系运算符		优先级： < > >= <= 大于 == != 
	>	//大于 
	>=	//大于等于 
	<	//小于 
	<=	//小于等于 
	==	//等于 
	!= 	//不等于 
	
	//算术运算符 		优先级：() 大于 */% 大于 +- 
	+	//加（a + b 
	-	//减 
	*	//乘 
	/	//除 
	%	//取余 
	i++ or i--	//变量先行，然后自增/减1 （先显示原始值 
	++i or --i 	//反的。
	
	//逻辑运算符 		优先级： 按先后顺序 
	&&	//与 满足多个条件 
	||	//或 满足其中一个 
	! 	//非 结果相反
	
	if(b == 1){					//if(条件)
		cout << "b=1";			//结果 
	} 
	if else(b == 2){
		cout << "b=2";
	}
	else{						//最后一条用else首尾 
		cout << "<( ￣^￣)(θ(θ☆( >_<";
	}
	
	
	while(表达式){				//先判断后再执行循环
								//while(1)即一直执行，(0)则不执行 
		//循环体
		break;					//跳出循环
		continue;				//跳过一次 
	} 
	
	do{							//先执行一次再判断 
		//循环体 
	}while (表达式); 
	
	for (int i=1;i <= 10;i++){	//(初始设定;循环条件;结束动作【初始化->判断->循环体->结束动作】)
		//循环体 
	}
	return 0;					//结束 
} 
